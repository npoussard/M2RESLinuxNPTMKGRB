---
- hosts: srv-web 
  vars_files:
          - vars/main.yml
  pre_tasks:
          - name: création de documentclient
            file: 
             path: "{{ item.documentclient }}" 
             state: directory
            with_items: "{{ apache_vhosts }}"
  roles:
          - { role: geerlingguy.apache }
